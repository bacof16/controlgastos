# Imagen base - nginx para servir archivos estáticos
FROM nginx:alpine

# Copiar archivos estáticos al directorio por defecto de nginx
COPY . /usr/share/nginx/html

# Exponer puerto 80
EXPOSE 80

# Nginx se inicia automáticamente
